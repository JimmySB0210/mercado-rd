export function CategoryBar({ active }: { active?: string }) {
  return (
    <div style={{background:'linear-gradient(180deg,#e8f0fe 0%,#f0f4ff 40%,#f8f9ff 75%,#ffffff 100%)',paddingBottom:8}}>
    </div>
  );
}